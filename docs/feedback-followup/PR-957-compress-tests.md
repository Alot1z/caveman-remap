# PR #957 — test(compress): regression tests for first-text-block selection

Status: **MERGED** (2026-09-03, squash `1360b60`).

## Feedback

No reviewer comments beyond the bot's usage-limit notice. The PR is the
missing regression coverage for the #932 crash fix (shipped upstream as
v2.5.0 d6c1cd3).

## Disposition

Merged; the #932 feedback loop is closed (fix + tests both landed).

## Plan (draft)

None. This entry exists to complete the matrix.

## Lesson (recorded)

When upstream lands the minimal fix, the follow-up PR that supplies the
missing tests is a welcome shape (tests-only, small, no behavior change).